document.addEventListener('keydown', (event) => {
    // Check if the 'E' key is pressed (you can customize the key if needed)
    if (event.key === 'e') {
        // Simulate going back outside (to gta.html)
        window.location.href = 'gta.html';
    }
});

